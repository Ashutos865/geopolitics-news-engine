name: Global News Update

on:
  schedule:
    - cron: '2,7,12,17,22,27,32,37,42,47,52,57 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    
    # This ensures everything runs inside your RSS folder
    defaults:
      run:
        working-directory: RSS

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install aiohttp feedparser pandas trafilatura

      - name: Run Scraper
        run: python scraper.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "actions@github.com"
          
          # We need to move back to root to push, but add the files in RSS
          cd ..
          git pull origin main --rebase 
          git add RSS/seen_links.json RSS/raw_news.json
          
          git commit -m "Auto-update: Tactical Intelligence Sync [$(date)]" || exit 0
          git push origin main